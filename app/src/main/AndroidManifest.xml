<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/doo_gdh_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/doo_gdh_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GDH559"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainComingSoon" />

        <activity android:name=".MainAbout" />

        <activity android:name=".MainWatchingRunRun" />

        <activity android:name=".MainWatchingLahnma" />

        <activity android:name=".MainWatchingWimarnNarm" />

        <activity android:name=".MainWatchingNotFriends" />

        <activity android:name=".MainWatchingHomeForRent" />

        <activity android:name=".MainWatchingRaegYam" />

        <activity android:name=".MainWatchingBadGenius" />

        <activity android:name=".MainWatchingBro" />

        <activity android:name=".MainWatchingConheartist" />

        <activity android:name=".MainWatchingFanday" />

        <activity android:name=".MainWatchingTheMedium" />

        <activity android:name=".MainWatchingHomestay" />

        <activity android:name=".MainWatchingDestiny" />

    </application>

</manifest>